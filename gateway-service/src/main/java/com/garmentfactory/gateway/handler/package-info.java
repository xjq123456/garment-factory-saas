/**
 * WebFlux endpoints owned by the gateway module (for example auxiliary HTTP routes).
 * Prefer gateway predicates/filters for cross-cutting behavior.
 */
package com.garmentfactory.gateway.handler;
